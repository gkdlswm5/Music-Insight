import React from "react";
import { Container } from "react-bootstrap";

function Info({ musicData, track }) {
  console.log(musicData);

  var bars = [];

  //INITIALIZING CHART
  //   var myBarChart = new Chart(ctx, {
  //     type: 'bar',
  //     data: track,
  //     options: options
  // });

  //   for (var propoert in musicData)
  return (
    <Container>
      <Container>11</Container>
    </Container>
  );
}

export default Info;
